# AUTOGENERATED FILE: DO NOT EDIT
# Generated by the AgentRegistry CLI.

services:
  dice-agent:
    image: localhost:5001/dice-agent:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: ["dice-agent", "--local"]
    ports:
      - "8080:8080"
    environment:
      # TODO: remove kagent variables (currently required)
      - KAGENT_URL=placeholder
      - KAGENT_NAME=placeholder
      - KAGENT_NAMESPACE=placeholder
      - AGENT_NAME=dice-agent
      - MODEL_PROVIDER=gemini
      - MODEL_NAME=gemini-2.0-flash
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      - type: bind
        source: ./dice-agent
        target: /config

